# EE Gateway: Umbrel app manifest
# Copyright (C) 2026 encryptedenergy.com
# SPDX-License-Identifier: GPL-3.0-only
#
# Listing metadata shown in the umbrelOS UI. The app is distributed through the
# encryptedenergy community app store, so the id is prefixed with that store id
# (store id "encryptedenergy" + app "ee-gateway" => "encryptedenergy-ee-gateway").
#
# The icon and gallery URLs point at this community app store repo (org
# Encrypted-Energy, branch main), where this folder is published. They are
# live and return HTTP 200.
manifestVersion: 1
id: encryptedenergy-ee-gateway
name: EE Gateway
tagline: Self-hosted Hubble Network Bluetooth gateway
icon: https://raw.githubusercontent.com/Encrypted-Energy/umbrel-community-app-store/main/encryptedenergy-ee-gateway/icon.svg
category: Networking
version: "0.6.3"
releaseNotes: >-
  Two UX fixes from the first real-hardware install. The setup wizard
  now shows "Verifying credentials" for up to a minute after submission
  so newly-saved credentials never read as "missing" while the worker
  is picking them up on its next scan cycle. The dashboard adds a
  "Restart worker" button: useful right after plugging in a USB
  dongle, or any time the worker container needs a clean kick without
  opening a terminal. New worker image 0.6.3 and UI image 0.4.0;
  manifest moves to 0.6.3.
port: 8080
description: >-
  EE Gateway turns your Umbrel into a self-hosted Hubble Network Bluetooth
  gateway.


  It scans with your device's Bluetooth radio for nearby Hubble Network devices
  and forwards their packets to the Hubble cloud, extending coverage for the
  global Hubble Network from hardware you already own.


  Two containers do the work: a worker that scans over Bluetooth and ingests
  packets to Hubble, and a small web dashboard for first-time setup and live
  status. Enter your Hubble organization ID and API token once in the setup
  wizard, and the gateway runs on its own.


  EE Gateway is free and open-source software (GPL-3.0-only), built by
  encryptedenergy.com. It is an independent community project and is not an
  official Hubble Network product.
developer: encryptedenergy.com
website: https://encryptedenergy.com
submitter: John Marbach
submission: https://github.com/Encrypted-Energy/gateway
repo: https://github.com/Encrypted-Energy/gateway
support: https://github.com/Encrypted-Energy/gateway/issues
gallery:
  - https://raw.githubusercontent.com/Encrypted-Energy/umbrel-community-app-store/main/encryptedenergy-ee-gateway/gallery/1-setup.png
  - https://raw.githubusercontent.com/Encrypted-Energy/umbrel-community-app-store/main/encryptedenergy-ee-gateway/gallery/2-dashboard.png
  - https://raw.githubusercontent.com/Encrypted-Energy/umbrel-community-app-store/main/encryptedenergy-ee-gateway/gallery/3-status.png
dependencies: []
path: ""
defaultUsername: ""
defaultPassword: ""
